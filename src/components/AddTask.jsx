import { Button } from '@mui/material'
import React from 'react'

const AddTask = () => {
  return (
    <form>
      <input type='' placeholder='add task' />
      < Button variant= 'contained'> Saved Task </Button>
    </form>
  )
}

export default AddTask
